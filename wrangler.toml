name = "cftelebot"
main = "src/index.ts"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# 启用日志
[observability]
enabled = true

# 环境变量在 Cloudflare Dashboard 中配置
# 变量名：TELEGRAM_BOT_TOKEN, REDIS_ENDPOINT, REDIS_TOKEN, ALLOW_USERIDS, API_TOKEN
